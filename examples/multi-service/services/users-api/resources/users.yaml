resource: users
version: 1

schema:
  id:         { type: uuid, primary: true, generated: true }
  email:      { type: string, format: email, unique: true, required: true }
  name:       { type: string, min: 1, max: 200, required: true }
  role:       { type: enum, values: [admin, member, viewer], default: member }
  created_at: { type: timestamp, generated: true }
  updated_at: { type: timestamp, generated: true }

endpoints:
  list:
    auth: [member, admin]
    filters: [role]
    search: [name, email]
    pagination: cursor

  get:
    auth: [member, admin]

  create:
    auth: [admin]
    input: [email, name, role]
    controller: { before: prepare_user, after: provision_defaults }

  update:
    auth: [admin, owner]
    input: [name, role]
    controller: { before: validate_user_update }
